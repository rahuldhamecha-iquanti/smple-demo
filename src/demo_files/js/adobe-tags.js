$(window).on('load', function() {

    $('.wrap_1 .ApplyNow').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Meet The Cards Hilton Honors American Express Card",
            eventType: "pageInteraction"
            
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

       $('.wrap_2 .ApplyNow').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Meet The Cards Hilton Honors American Express Surpass Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('.wrap_3 .ApplyNow').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Meet The Cards Hilton Honors American Express Aspire Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_4 .ApplyNow').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Meet The Cards Hilton Honors American Express Business Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_1 .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Meet The Cards Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('.wrap_1 .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Meet The Cards Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_1 .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Meet The Cards Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_2 .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Meet The Cards Hilton Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('.wrap_2 .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Meet The Cards Hilton Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_2 .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Meet The Cards Hilton Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_3 .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Meet The Cards Hilton Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('.wrap_3 .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Meet The Cards Hilton Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_3 .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Meet The Cards Hilton Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
    $('.wrap_4 .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Meet The Cards Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('.wrap_4 .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Meet The Cards Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.wrap_4 .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Meet The Cards Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
  $('.stickyApply').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply for a Card Nav",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
  $('.stickyCompare').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Compare Cards Nav",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
  $('.stickyExplore').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Explore The Cards Nav",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#header-logo').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Meet The Cards Business",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

  $('#hilton_honors .ApplyNow').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Hilton Honors American Express Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
            
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#surpass .ApplyNow').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Hilton Honors American Express Surpass Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#aspire .ApplyNow').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Hilton Honors American Express Aspire Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#business .ApplyNow').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Hilton Honors American Express Business Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#hilton_honors .offerTerms').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card - Offer Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#hilton_honors .ratesFees').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card - Rates & Fees Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#hilton_honors .benefitTerms ').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card - Benefit Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#surpass .offerTerms').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card - Offer Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#surpass	.ratesFees').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card - Rates & Fees Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#surpass .benefitTerms ').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card - Benefit Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#aspire .offerTerms').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card - Offer Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#aspire .ratesFees').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card - Rates & Fees Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#aspire .benefitTerms ').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card - Benefit Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#business .offerTerms').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card - Offer Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#business .ratesFees').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card - Rates & Fees Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#business .benefitTerms ').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card - Benefit Terms Compare Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});   

//compare card selected cards

    //compare buttons
var busch;
var aspch;
var surch;
var basch;
    $('#HonorsChecked').on('click', function() {
if($('#HonorsChecked:checked').is(':checked')) {
    var basch="Base Checked";
}  
});

    $('#SurpassChecked').on('click', function() {
    if($('#SurpassChecked:checked').is(':checked')) {
    var surch="Surpass Checked";
}      
});
    $('#AspireChecked').on('click', function() {
        if($('#AspireChecked:checked').is(':checked')) {
    var aspch="Aspire Checked";
}      
});
    $('#BusinessChecked').on('click', function() {
         if($('#BusinessChecked:checked').is(':checked')) {
    var busch="Business Checked";
}  
});
$('#comparenow').on('click', function() {
    var cardtype= $('input[name="card_type"]:checked').val() || '';
    var spend= $('input[name="spend"]:checked').val() || '';
    var most_important= $('input[name="most_important"]:checked').val() || '';
    let compareCards = [];
    $("#HonorsChecked").is(':checked') ? compareCards.push("Base") : null;
    $("#SurpassChecked").is(':checked') ? compareCards.push("Surpass") : null;
    $("#AspireChecked").is(':checked') ? compareCards.push("Aspire") : null;
    $("#BusinessChecked").is(':checked') ? compareCards.push("Business") : null;
    console.log(compareCards)
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Compare Now Cards - " + compareCards,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#hilton_honors .benefits_btn').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#surpass .benefits_btn').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Hilton Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#aspire .benefits_btn').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Hilton Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#business .benefits_btn').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.range_4').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Hilton Honors Points 4 nights card Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.range_3').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Hilton Honors Points 3 nights  Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.range_2').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Hilton Honors Points 2 nights  Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.range_1').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Hilton Honors Points 1 nights  Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#sliderRange').on('change', function() {
    if($('.range_1').hasClass("selected"))
    {
        var nights="1 night";
    }
        if($('.range_2').hasClass("selected"))
    {
            var nights="2 night";
    }
        if($('.range_3').hasClass("selected"))
    {
            var nights="3 night";
    }
        if($('.range_4').hasClass("selected"))
    {
            var nights="4 night";
    }
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Hilton Honors Points Slider "+nights,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#member').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Elite Status member card Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#silver').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Elite Status silver card Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#gold').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Elite Status gold card  Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#diamond').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Elite Status diamond card Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_hilton .benefits_btn').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Popup Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_surpass .benefits_btn').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Popup Hilton Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_aspire .benefits_btn').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Popup Hilton Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_business .benefits_btn').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "More Benefits Compare card Popup Hilton Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_hilton .offerTerms').on('click', function() {
    
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Compare Card Popup Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#pop_hilton .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Compare Card Popup Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_hilton .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Compare Card Popup Hilton Honors American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_surpass .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Compare Card Hilton Popup Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#pop_surpass	.ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Compare Card Hilton Popup Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_surpass .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Compare Card Hilton Popup Honors Surpass American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_aspire .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Compare Card Hilton Popup Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#pop_aspire .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Compare Card Hilton Popup Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_aspire .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Compare Card Hilton Popup Honors Aspire American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
    $('#pop_business .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Offer Terms Compare Card Hilton Popup Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#pop_business .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Rates & Fees Compare Card Hilton Popup Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_business .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Benefit Terms Compare Card Hilton Popup Honors Business American Express Card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#hhid-submit').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Visible Interstitial View Offer",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#qhhnumber').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Visible Interstitial How do I find my RSVP code",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
}); 
$('.not_a_link').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Visible Interstitial How do I find my RSVP code",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});   
$('.ReturnBtn').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Return to compare card compare card popup",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});  
$('.clearfilter').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Clear all filters compare card section",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});   

  $('#pop_hilton .ApplyNow').on('click', function() {
          var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Pop up Hilton Honors American Express Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
            
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

       $('#pop_surpass .ApplyNow').on('click', function() {
               var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Pop up Hilton Honors American Express Surpass Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#pop_aspire .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Pop up Hilton Honors American Express Aspire Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#pop_business .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Pop up Hilton Honors American Express Business Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
    $('#pop_business .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
var spend=$('input[name="spend"]:checked').val();
var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Apply Now Compare Card Pop up Hilton Honors American Express Business Card "+cardtype+" "+spend+" "+most_important,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
//banner
//     $('#banner_base').on('click', function() {
//         var cardtype=$('input[name="card_type"]:checked').val();
// var spend=$('input[name="spend"]:checked').val();
// var most_important=$('input[name="most_important"]:checked').val();
//     if (typeof(digitalData.event) != 'undefined') {
//         digitalData.event.length = 0;
//     }
//     digitalData.event.push({
//         eventInfo: {
//             eventAction: "Click",
//             eventAsset: "Banner Click Honors American Express Card ",
//             eventType: "pageInteraction"
//         }
//     });
//     if (typeof(_satellite) != "undefined") {
//         _satellite.track("event");
//     }
// });
//     $('#banner_surpass').on('click', function() {
//         var cardtype=$('input[name="card_type"]:checked').val();
// var spend=$('input[name="spend"]:checked').val();
// var most_important=$('input[name="most_important"]:checked').val();
//     if (typeof(digitalData.event) != 'undefined') {
//         digitalData.event.length = 0;
//     }
//     digitalData.event.push({
//         eventInfo: {
//             eventAction: "Click",
//             eventAsset: "Banner Click Hilton Honors American Express Surpass Card ",
//             eventType: "pageInteraction"
//         }
//     });
//     if (typeof(_satellite) != "undefined") {
//         _satellite.track("event");
//     }
// });
//     $('#banner_aspire').on('click', function() {
//         var cardtype=$('input[name="card_type"]:checked').val();
// var spend=$('input[name="spend"]:checked').val();
// var most_important=$('input[name="most_important"]:checked').val();
//     if (typeof(digitalData.event) != 'undefined') {
//         digitalData.event.length = 0;
//     }
//     digitalData.event.push({
//         eventInfo: {
//             eventAction: "Click",
//             eventAsset: "Banner Click Hilton Honors American Express Asipre Card ",
//             eventType: "pageInteraction"
//         }
//     });
//     if (typeof(_satellite) != "undefined") {
//         _satellite.track("event");
//     }
// });
//     $('#banner_business').on('click', function() {
//         var cardtype=$('input[name="card_type"]:checked').val();
// var spend=$('input[name="spend"]:checked').val();
// var most_important=$('input[name="most_important"]:checked').val();
//     if (typeof(digitalData.event) != 'undefined') {
//         digitalData.event.length = 0;
//     }
//     digitalData.event.push({
//         eventInfo: {
//             eventAction: "Click",
//             eventAsset: "Banner Click Hilton Honors American Express Business Card ",
//             eventType: "pageInteraction"
//         }
//     });
//     if (typeof(_satellite) != "undefined") {
//         _satellite.track("event");
//     }
// });

    $('#banner_base > .mobilebanner > .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
        var spend=$('input[name="spend"]:checked').val();
        var most_important=$('input[name="most_important"]:checked').val();
        if (typeof(digitalData.event) != 'undefined') {
            digitalData.event.length = 0;
        }
        digitalData.event.push({
            eventInfo: {
                eventAction: "Click",
                eventAsset: "Base Honors American Express Card - Mobile Banner - Apply now button",
                eventType: "pageInteraction"
            }
        });
        if (typeof(_satellite) != "undefined") {
            _satellite.track("event");
        }
    });
    $('#banner_surpass > .mobilebanner > .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
        var spend=$('input[name="spend"]:checked').val();
        var most_important=$('input[name="most_important"]:checked').val();
        if (typeof(digitalData.event) != 'undefined') {
            digitalData.event.length = 0;
        }
        digitalData.event.push({
            eventInfo: {
                eventAction: "Click",
                eventAsset: "Surpass Honors American Express Card - Mobile Banner - Apply now button",
                eventType: "pageInteraction"
            }
        });
        if (typeof(_satellite) != "undefined") {
            _satellite.track("event");
        }
    });
    $('#banner_aspire > .mobilebanner > .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
        var spend=$('input[name="spend"]:checked').val();
        var most_important=$('input[name="most_important"]:checked').val();
            if (typeof(digitalData.event) != 'undefined') {
                digitalData.event.length = 0;
            }
            digitalData.event.push({
                eventInfo: {
                    eventAction: "Click",
                    eventAsset: "Aspire Honors American Express Card - Mobile Banner - Apply now button",
                    eventType: "pageInteraction"
                }
            });
            if (typeof(_satellite) != "undefined") {
                _satellite.track("event");
            }
    });
    $('#banner_business > .mobilebanner > .ApplyNow').on('click', function() {
        var cardtype=$('input[name="card_type"]:checked').val();
        var spend=$('input[name="spend"]:checked').val();
        var most_important=$('input[name="most_important"]:checked').val();
        if (typeof(digitalData.event) != 'undefined') {
            digitalData.event.length = 0;
        }
        digitalData.event.push({
            eventInfo: {
                eventAction: "Click",
                eventAsset: "Business Honors American Express Card - Mobile Banner - Apply now button",
                eventType: "pageInteraction"
            }
        });
        if (typeof(_satellite) != "undefined") {
            _satellite.track("event");
        }
    });

$('#hgv_code').on('focusout', function() {
    var cardtype=$('input[name="card_type"]:checked').val();
    var spend=$('input[name="spend"]:checked').val();
    var most_important=$('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "RSVP code input Entered",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});


$('.prev').on('click', function() {
    const sliderNo = $(".card_Wrap").find('li.activeCard').data("slide");
    const card = (sliderNo == 1 ? "Base" : (sliderNo == 2 ? "Surpass" : (sliderNo == 3 ? "Aspire" : "Business")));
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Mobile Slider - Previous Btn - " + card + " card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});


$('.next').on('click', function() {
    const sliderNo = $(".card_Wrap").find('li.activeCard').data("slide");
    const card = (sliderNo == 1 ? "Base" : (sliderNo == 2 ? "Surpass" : (sliderNo == 3 ? "Aspire" : "Business")));
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Mobile Slider - Next Btn - " + card + " card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.dot').on('click', function() {
    const sliderNo = $(".card_Wrap").find('li.activeCard').data("slide");
    const card = (sliderNo == 1 ? "Base" : (sliderNo == 2 ? "Surpass" : (sliderNo == 3 ? "Aspire" : "Business")));
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Mobile Slider - Dots - " + card + " card",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_base > .mobilebanner > .Terms > .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card  - Mobile Banner- Offer Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#banner_base > .mobilebanner > .Terms > .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card  - Mobile Banner- Rates & Fees link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_base > .mobilebanner > .Terms > .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Base Hilton Honors American Express Card  - Mobile Banner- Benefit Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_surpass > .mobilebanner > .Terms > .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card  - Mobile Banner- Offer Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#banner_surpass > .mobilebanner > .Terms > .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card  - Mobile Banner- Rates & Fees link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_surpass > .mobilebanner > .Terms > .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Surpass Hilton Honors American Express Card  - Mobile Banner- Benefit Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_aspire > .mobilebanner > .Terms > .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card  - Mobile Banner- Offer Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#banner_aspire > .mobilebanner > .Terms > .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card  - Mobile Banner- Rates & Fees link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_aspire > .mobilebanner > .Terms > .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Aspire Hilton Honors American Express Card  - Mobile Banner- Benefit Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
    $('#banner_business > .mobilebanner > .Terms > .offerTerms').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card  - Mobile Banner- Offer Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});

$('#banner_business > .mobilebanner > .Terms > .ratesFees').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card  - Mobile Banner- Rates & Fees link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('#banner_business .mobilebanner .Terms > .benefitTerms ').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Business Hilton Honors American Express Card  - Mobile Banner- Benefit Terms link",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('.LearnMore').on('click', function() {
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "LearnMore DCLP",
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('input[name="card_type"]').on('click', function() {
    const cardType = $('input[name="card_type"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Quiz Q1 - Card Type - " + cardType,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('input[name="spend"]').on('click', function() {
    const spendType = $('input[name="spend"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Quiz Q2 - Spend Most - " + spendType,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
$('input[name="most_important"]').on('click', function() {
    const spendType = $('input[name="most_important"]:checked').val();
    if (typeof(digitalData.event) != 'undefined') {
        digitalData.event.length = 0;
    }
    digitalData.event.push({
        eventInfo: {
            eventAction: "Click",
            eventAsset: "Quiz Q3 - Most Important - " + spendType,
            eventType: "pageInteraction"
        }
    });
    if (typeof(_satellite) != "undefined") {
        _satellite.track("event");
    }
});
});